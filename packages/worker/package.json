{
  "name": "@vmcr/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply vmcr-sources",
    "db:migrate:local": "wrangler d1 migrations apply vmcr-sources --local",
    "seed": "node --loader ts-node/esm src/scripts/seed.ts"
  },
  "dependencies": {
    "@vmcr/shared": "*",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.99.0"
  }
}
